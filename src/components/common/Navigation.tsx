import React from 'react';

// Styles
import '../../assets/styles/navigation.scss';

export function Navigation(): JSX.Element {
    return (
        <nav>
            <h1>Trade Tracker</h1>
        </nav>
    );
}
